---
name: ImageSlider
component: true
---

<template>
  <div class="swiper-container">
    <div class="swiper-wrapper">
      <!-- Render each slide -->
      {#each images as image, i}
        <div class="swiper-slide">
          <img src={image.src} alt={image.alt} />
        </div>
      {/each}
    </div>

    <!-- Add navigation buttons -->
    <div class="swiper-button-prev"></div>
    <div class="swiper-button-next"></div>

    <!-- Add pagination -->
    <div class="swiper-pagination"></div>
  </div>
</template>

<style>
  .swiper-container {
    /* Add your styles for the slider container here */
  }

  .swiper-slide {
    /* Add your styles for each slide here */
  }

  /* Add styles for navigation buttons */
  .swiper-button-prev,
  .swiper-button-next {
    /* Add your styles for navigation buttons here */
  }

  /* Add styles for pagination */
  .swiper-pagination {
    /* Add your styles for pagination here */
  }
</style>

<script type="module">
export function onMount() {
    // Initialize Swiper
    new Swiper('.swiper-container', {
        // Customize Swiper options here
        navigation: {
            nextEl: '.swiper-button-next',
            prevEl: '.swiper-button-prev',
        },
        pagination: {
            el: '.swiper-pagination',
            clickable: true,
        },
    });
}
</script>
